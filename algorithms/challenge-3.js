function sumMix(arr) {
    function sumMix(arr) {
        let output = 0 
        for (let i = 0; i < arr.length; i++) {
          const element = Number(arr[i]);
          output+=element
      }
      return output
 }
}
  module.exports = sumMix