function filterList(arr) {
    function filterList(arr) {
        let newArr = arr.filter(element => typeof element !== 'string' )
      
        console.log(newArr);
      }
 }

  module.exports =filterList